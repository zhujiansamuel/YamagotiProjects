# =============================================================================
# YamagotiProjects 环境变量配置示例
# 复制此文件为 .env 并根据环境修改
# =============================================================================

# -----------------------------------------------------------------------------
# 外部 PostgreSQL 数据库配置
# -----------------------------------------------------------------------------
# docker-compose.yml 中的 pgbouncer 和所有服务都从这里读取连接信息
# 本地开发: PG_HOST=host.docker.internal, 连接宿主机上的 local-pg 容器
# 服务器部署: PG_HOST=host.docker.internal (Linux 需 extra_hosts 支持), 或服务器实际 IP
PG_HOST=host.docker.internal
PG_PORT=5433                        # 本地 local-pg 映射端口；服务器上通常为 5432
PG_DATABASE=applestockchecker_dev
PG_USER=postgres                    # 本地 local-pg 用户；服务器上改为实际用户
PG_PASSWORD=localpass               # 本地 local-pg 密码；服务器上改为实际密码

# -----------------------------------------------------------------------------
# Django 配置
# -----------------------------------------------------------------------------
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_ENV=dev

# -----------------------------------------------------------------------------
# Redis 配置
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# -----------------------------------------------------------------------------
# Celery 配置
# -----------------------------------------------------------------------------
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
CELERY_CONCURRENCY_DEFAULT=4
CELERY_CONCURRENCY_WEBSCRAPER=2

# -----------------------------------------------------------------------------
# WebScraper API 配置
# -----------------------------------------------------------------------------
WEB_SCRAPER_API_TOKEN=your-webscraper-api-token
WEB_SCRAPER_WEBHOOK_TOKEN=your-webhook-token

# -----------------------------------------------------------------------------
# 外部商品价格同步配置
# -----------------------------------------------------------------------------
EXTERNAL_GOODS_API_URL=http://localhost:8080
EXTERNAL_GOODS_API_TOKEN=your-external-api-token-here
EXTERNAL_GOODS_CATEGORY_FILTER=iPhone

# -----------------------------------------------------------------------------
# CORS 配置
# -----------------------------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# 其他配置
# -----------------------------------------------------------------------------
TREND_MAX_LOOKBACK_DAYS=90
TREND_DB_MAX_WORKERS=6
